%div.container
	temp: for devs

	/ %h1= "Favorited Projects"
	/ - if @favorites
	/   - @favorites.each do |f|
	/     =render :partial=>"shared/project_card", :locals => {:project => f}
	/   =will_paginate @favorites, :param_name => 'favorites_page'
<%= form_for(@email_notification) do |f| %>
  <% if @email_notification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_notification.errors.count, "error") %> prohibited this email_notification from being saved:</h2>

      <ul>
      <% @email_notification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :edit %><br />
    <%= f.text_field :edit %>
  </div>
  <div class="field">
    <%= f.label :update %><br />
    <%= f.text_field :update %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

.img-polaroid
  - if project.client.photo.exists?
    =link_to(image_tag(project.client.photo.to_s), project_path(project))
  - else
    =link_to(image_tag(ClientPhoto.default_url), project_path(project))
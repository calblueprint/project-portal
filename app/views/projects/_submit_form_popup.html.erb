<div id="submit-form-popup-<%=project.slug%>" class="modal hide" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">
      <%= "Submitting: #{project.title}" %>
    </h3>
  </div>
  <div class="modal-body">
    <%= simple_form_for project do |f| %>
    <%= f.hidden_field :state, value: Project::FINISHED %>
    Submitting a project can only be done once and should be done once the project is complete. Are you sure you want to submit this project?
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <%= f.button :submit, "Submit", class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>